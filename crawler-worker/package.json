{
  "name": "digibility-seo-crawler-worker",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Digibility SEO crawler worker. Phase 1C/1D discovery, extraction, deterministic issue detection and publishing over real HTTP (DiscoveryProcessor). Non-test jobs are refused unless CRAWLER_ALLOW_NON_TEST_JOBS is set.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "node --import tsx --test test/*.test.ts",
    "start": "tsx src/index.ts",
    "dry-run": "tsx src/index.ts --mode=dry-run"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.50.3",
    "fast-xml-parser": "^5.10.0",
    "node-html-parser": "^6.1.13"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.0"
  }
}
