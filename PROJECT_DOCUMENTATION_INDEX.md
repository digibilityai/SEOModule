# Project Documentation Index

**Purpose:** the map of every documentation file in this repo — what each is
for, its current accuracy status, which stage/phase it belongs to, and when to
read it. Maintained per `DOCUMENTATION_WORKFLOW_RULES.md`.

**Last audited:** 2026-07-10 (full documentation audit, end of Phase 14B.2).
**Last consolidated:** 2026-07-20 (authoritative context package created — see below).

## ★ Authoritative context package (READ FIRST — 2026-07-20 consolidation)

A new ChatGPT or Claude session should be bootstrapped from these **four
authoritative files**, in this reading order. They are the current authority for
product context, implementation/lock/production state, and decisions; they
supersede the general-handover role of the older files (which are retained as
historical/evidence — see the tables below and the redirect banners in those
files).

| # | Authoritative file | Authority (what it is the source of truth for) |
|---|---|---|
| 1 | **`SEO_CONTEXT_HANDOVER.md`** | **Primary entry point** — current stage, exact next step, session bootstrap instructions, latest reconciliation. |
| 2 | **`SEO_IMPLEMENTATION_STATUS.md`** | Concise current implementation + lock + TEST/production state; completion evidence pointers; recommended next workstream + first step. |
| 3 | **`SEO_PROJECT_CONTEXT.md`** | Product purpose/scope, architecture, BFF/trusted-boundary model, domain boundaries, conventions, collaboration + doc-authority rules. |
| 4 | **`SEO_DECISIONS.md`** | Current confirmed architectural/security/concurrency/BFF/lock/promotion decisions + rejected/superseded alternatives. |

**Full authority hierarchy (highest first):**
`SEO_CONTEXT_HANDOVER.md` → `SEO_IMPLEMENTATION_STATUS.md` → `SEO_PROJECT_CONTEXT.md`
→ `SEO_DECISIONS.md` → `MODULE_LOCKS.md` → module-specific sign-off & verification
evidence (P1a/16C–16H/Stage 6/P1b sign-offs + `supabase/` migration/verification/
rollback artifacts + `P1B_CONCURRENCY_VERIFICATION_GUIDE.md`) →
`CURRENT_PROJECT_STATUS.md` (detailed dated ledger, retained) → historical/archive
docs.

**Superseded as *general handover* (retained as historical evidence, not deleted):**
`CHATGPT_CONTEXT_HANDOVER.md` (now carries a redirect banner to
`SEO_CONTEXT_HANDOVER.md`) and `BACKEND_MILESTONE_HANDOFF.md` (retained for its §5
production-gating checklist + backend milestone history). `PROJECT_BOOTSTRAP.md`,
`PROJECT_CONTEXT.md`, `SEO_PRODUCT_BLUEPRINT_REAL_APP.md`, and
`SUPABASE_BACKEND_ARCHITECTURE_PLAN.md` remain valid **reference** docs; their
current-context role is now carried by `SEO_PROJECT_CONTEXT.md`.

**No files were deleted in the 2026-07-20 consolidation** (preservation-first: no
lock/sign-off/migration/rollback/verification/concurrency evidence was removed;
uncertain items were retained/archived-in-place rather than deleted). The stale
pre-execution note in `P1B_VERIFIED_ONLY_CRAWL_ENQUEUE_PLAN.md` was labelled
**SUPERSEDED** (additive; historical text preserved).

---

> **Legacy preflight note (still valid):** `PROJECT_BOOTSTRAP.md` (orientation),
> `CURRENT_PROJECT_STATUS.md` (detailed dated status), this index, and
> `DOCUMENTATION_WORKFLOW_RULES.md` remain useful — but for a *new session* start
> from the authoritative package above first.

**Status legend:**
- **current** — accurate, no change needed in this audit.
- **updated** — an already-accurate doc that this audit extended/refined.
- **stale-fixed** — this audit corrected outdated status claims.
- **new** — created in this audit.
- **reference** — a product/spec/instruction doc with no implementation-status
  claims (stays valid regardless of build progress).

---

## A. Start-here / status (read first, all tasks)

| File | Purpose | Status | Read before |
| --- | --- | --- | --- |
| `PROJECT_BOOTSTRAP.md` | Single entry point for any new AI session — project overview, repo rules, mandatory reading order, locked architectural decisions, module map, module completion/lock rules, AI working agreement, bootstrap prompt. | **new** | **Every task, first** |
| `CHATGPT_CONTEXT_HANDOVER.md` | Compact, self-contained handover for a fresh **ChatGPT oversight thread** — product/repo overview, architecture + BFF boundaries (no-BFF Supabase-direct; crawler worker = trusted-server exception; parent-platform BFF deferred), backward-compat, source-of-truth hierarchy, dev plan/phase, locked modules, **latest P1a state (COMPLETE and MODULE-LOCKED 2026-07-19: Step 2.8 PASS, browser role matrix PASS, real `verify-once` worker-binary run PASS; P1b is now the next stage)**, accepted/rejected decisions, exact next step, the ChatGPT↔Claude protocol + Claude model-selection protocol, and the minimum upload set. | **updated** (2026-07-19 — P1a lock reflected) | Handing the project to a new ChatGPT thread |
| `MODULE_LOCKS.md` | Authoritative per-module lock registry — locked file lists, allowed/not-allowed changes, evidence bar required before modifying a locked module. Includes **Page Performance Tracker**, **Stage 6 — Off-Page Authority Workflows and AI Visibility Reads** (LOCKED 2026-07-13), **Crawler customer UI + crawl/audit/publishing contracts (Phase 16C–16H implemented scope)** (LOCKED 2026-07-15), and **P1a — Domain Ownership Verification** (LOCKED 2026-07-19, DNS-TXT Steps 1–6; P1b verified-only crawl enqueue enforcement remains UNLOCKED/unimplemented). | **updated** (2026-07-19 — P1a implemented-scope lock added) | Before modifying any file belonging to a listed module |
| `CURRENT_PROJECT_STATUS.md` | Authoritative one-page current status (backend/wiring/seed/live-test/production/limitations/next step). | **new** | **Every task** |
| `PROJECT_DOCUMENTATION_INDEX.md` | This map of all docs. | **new** | **Every task** |
| `DOCUMENTATION_WORKFLOW_RULES.md` | Preflight + docs-sync rules for all future tasks. | **new** | **Every task** |
| `README.md` | Repo entry point: stack, setup, data mode, status summary. | **stale-fixed** (was stuck at "Phase 0 scaffold") | Onboarding / setup |
| `CLAUDE.md` | Build instructions/rules for the coding agent. | **reference** | Every task |
| `PROJECT_CONTEXT.md` | Product scope, business rules, roles, v1 features. | **reference** | Product/scope decisions |
| `SEO_PRODUCT_BLUEPRINT_REAL_APP.md` | Product blueprint / source context for the real app. | **reference** | Product/design decisions |

---

## B. Backend — handoff & architecture (read before backend/production tasks)

| File | Purpose | Stage | Status | Read before |
| --- | --- | --- | --- | --- |
| `BACKEND_MILESTONE_HANDOFF.md` | Single-file backend status handoff + production gating checklist (§5). | Stages 1–5 | **stale-fixed** (Exec summary/§9/§10/§13 reconciled to Stage 5 + wiring done) | Backend / production |
| `SUPABASE_BACKEND_ARCHITECTURE_PLAN.md` | Overall backend architecture/design plan. | All | **current** (planning doc) | Backend design |

---

## C. Backend — per-stage migration plans & notes (read before touching that stage)

| File | Purpose | Stage | Status |
| --- | --- | --- | --- |
| `SUPABASE_MIGRATION_STAGE_1_NOTES.md` | Stage 1 implementation notes + TEST verification. | 1 | **current** |
| `SUPABASE_MIGRATION_STAGE_2_PLAN.md` | Stage 2 design plan. | 2 | **current** (planning) |
| `SUPABASE_MIGRATION_STAGE_2_NOTES.md` | Stage 2 notes + TEST verification. | 2 | **current** |
| `SUPABASE_MIGRATION_STAGE_3_CONTENT_STUDIO_PLAN.md` | Stage 3 design plan. | 3 | **current** (planning) |
| `SUPABASE_MIGRATION_STAGE_3_CONTENT_STUDIO_NOTES.md` | Stage 3 notes + TEST verification. | 3 | **current** |
| `SUPABASE_MIGRATION_STAGE_4_PAGE_PERFORMANCE_PLAN.md` | Stage 4 design plan (marked implemented + test-verified). | 4 | **current** |
| `SUPABASE_MIGRATION_STAGE_4_PAGE_PERFORMANCE_NOTES.md` | Stage 4 notes + TEST verification. | 4 | **current** |
| `SUPABASE_MIGRATION_STAGE_5_DECLINE_DIAGNOSIS_PLAN.md` | Stage 5 design plan. | 5 | **stale-fixed** (added applied/seeded/wired status banner; kept design record) |
| `SUPABASE_MIGRATION_STAGE_5_DECLINE_DIAGNOSIS_NOTES.md` | Stage 5 notes + §12 TEST verification checkpoint. | 5 | **current** |
| `SUPABASE_MIGRATION_STAGE_6_OFFPAGE_AI_VISIBILITY_PLAN.md` | Stage 6 design plan — Off-Page Authority + AI Visibility/GEO, with **§0 Locked Decisions** (SQL-ready spec: 8 tables + 2 transition RPCs). | 6 | **current** (decisions locked; SQL now authored per NOTES) |
| `SUPABASE_MIGRATION_STAGE_6_OFFPAGE_AI_VISIBILITY_NOTES.md` | Stage 6 implementation notes — the 7 migration files (`…120017`–`…120023`) + §7a/§7b dry-run + §7c TEST-apply + §7d smoke-test + §7e UI-seed checkpoints. **APPLIED to TEST (`Digi_SEO_Test`) + structurally verified + smoke-tested PASS + UI-seeded (9/4/11/6/5/9/6/13); still NOT service-wired.** | 6 | **updated** |

---

## D. Verification & seed guides (read before seed/verify tasks)

| File | Purpose | Stage | Status |
| --- | --- | --- | --- |
| `SUPABASE_STAGE_2_VERIFICATION_GUIDE.md` | How to verify Stage 2 backend on a TEST project. | 2 | **current** |
| `SUPABASE_STAGE_3_CONTENT_STUDIO_VERIFICATION_GUIDE.md` | How to verify Stage 3 backend on a TEST project. | 3 | **current** |
| `SUPABASE_UI_TEST_DATASET_SEED_GUIDE.md` | Base UI seed dataset guide (`seo_seed_ui_test_dataset.sql`). | 1–3 | **current** |
| `SUPABASE_STAGE4_PAGE_PERFORMANCE_SEED_EXTENSION_GUIDE.md` | Stage 4 UI seed extension guide. | 4 | **stale-fixed** (§8 now "wiring complete", not "not happened yet") |
| `SUPABASE_STAGE5_DECLINE_DIAGNOSIS_SEED_EXTENSION_GUIDE.md` | Stage 5 UI seed extension guide (8/20/6 counts). | 5 | **updated** (added applied/verified status banner) |
| `SUPABASE_STAGE6_OFFPAGE_AI_VISIBILITY_SEED_EXTENSION_GUIDE.md` | Stage 6 UI seed extension guide (`a6000000-` prefix; 9/4/11/6/5/9/6/13 counts; TEST/demo/manual_seed only). | 6 | **new** (applied + verified on TEST) |
| `supabase/test/seo_stage6_authority_campaign_create_verification.sql` | Phase 15D TEST-only verification for the `seo_authority_campaign_create` RPC (migration `…120024`) — 13 scenarios incl. role gating, draft status, junction/task rows, cross-workspace + invalid-owner rejection, dedup, forced-failure atomicity, teardown (`a7000000-` prefix, self-cleaning). | 6 (Phase 15D) | **new** (PASS on TEST 2026-07-12) |
| `supabase/test/seo_stage6_authority_campaign_transition_verification.sql` | Phase 15E TEST-only verification for the existing `seo_authority_campaign_transition` RPC (migration `…120020`) — all 4 legal transitions (+ the extra pending_approval→draft) per role, activity-row correctness (subject/action/from/to/actor_role_snapshot), 9 illegal-transition rejections, data-integrity invariants, RLS-enforced append-only activity, teardown (`a8000000-` prefix, self-cleaning, idempotent). No migration/RLS/seed/frontend changed — verifies the already-applied RPC only. | 6 (Phase 15E) | **new** (PASS on TEST 2026-07-12) |

---

## E. Service-layer wiring (read before service-wiring / UI tasks)

| File | Purpose | Phase | Status |
| --- | --- | --- | --- |
| `SERVICE_LAYER_WIRING_PLAN.md` | Master wiring plan + per-phase status (§8–§17). | 13A–15C | **updated** (§1 Current Mode refreshed; §16 Off-Page Authority + AI Visibility reads wired; §17 Opportunity Workflow writes signed off, campaign writes + AI Visibility writes still mock-only) |
| `PHASE_13B_SERVICE_WIRING_NOTES.md` | Website + Business Onboarding wiring. | 13B | **current** (historical) |
| `PHASE_13B1_DEV_AUTH_TEST_NOTES.md` | Dev-only Supabase Auth Test harness. | 13B.1 | **current** |
| `PHASE_13C_AUDIT_RECOMMENDATION_WIRING_NOTES.md` | Audit + Recommendation wiring. | 13C | **current** |
| `PHASE_13D_APPROVAL_QUEUE_WIRING_NOTES.md` | Approval Queue wiring (transition RPC). | 13D | **current** |
| `PHASE_13E_CONTENT_STUDIO_WIRING_NOTES.md` | Content Studio wiring (transition RPC). | 13E | **current** |
| `PHASE_13F_DASHBOARD_ADMIN_READONLY_WIRING_NOTES.md` | Dashboard summaries + Admin Preview (read-only). | 13F | **current** |
| `PHASE_14A_PAGE_PERFORMANCE_WIRING_NOTES.md` | Page Performance service wiring. | 14A.2 | **stale-fixed** (forward-refs added where it said "Decline Diagnosis not wired") |
| `PHASE_14B_DECLINE_DIAGNOSIS_WIRING_NOTES.md` | Decline Diagnosis service wiring + §10 finder-ranking fix + §11 onboarding-gate fix. | 14B.2 | **current** |
| `PHASE_15A_STAGE6_OFFPAGE_AI_VISIBILITY_WIRING_NOTES.md` | Off-Page Authority + AI Visibility/GEO service wiring (reads only; §7 explains why writes stay mock-only; §10 live-test steps). | 15A | **new** — implemented, pending live test |
| `PHASE_15B_STAGE6_WRITE_UX_AUDIT.md` | Audit-only: every current mock-only Off-Page/AI Visibility UI write action vs. the Stage 6 transition RPCs' legal state machine; proposed (not built) legal action matrix + unresolved UX decisions. **No code, migration, seed, or RLS changed.** | 15B | **new** — audit only, no writes wired |
| `PHASE_15C_OPPORTUNITY_WRITE_SIGNOFF.md` | Sign-off record for Off-Page Authority **Opportunity Workflow** writes (`seo_authority_opportunity_transition`): scope, files, legal state matrix, role-gating rules, non-masking error behavior, authenticated TEST validation results, persistence + activity-log verification, mock-mode status, known limitations, remaining Stage 6 work, production status. Campaign writes and AI Visibility remain out of scope/mock-only. | 15C | **new** — Opportunity Workflow implemented + signed off + live-tested; broader Off-Page Authority module still NOT LOCKED |
| `PHASE_15D_CAMPAIGN_WORKFLOW_SIGNOFF.md` | Sign-off record for Off-Page Authority **Campaign Workflow** writes (creation via `seo_authority_campaign_create`; approval state machine via `seo_authority_campaign_transition`): TEST env + project ref, authenticated 4-role results (owner/admin/team_member/client), the original client create-gating defect + the frontend-only fix (`RoleGateTooltip.tsx` + role-gated selection/Create), legal + denied coverage, persistence + activity-log + role-snapshot/`created_by` evidence, linked-opportunity/task integrity, double-submit protection, `tsc`/`build` + mock-mode results, known benign observations (favicon 404, sign-out `ERR_ABORTED`, ~20px mobile overflow), honest tooltip wording, files changed, rollback plan, remaining Stage 6 work, production untouched. | 15D | **new** — Campaign Workflow implemented + fully authenticated-validated + signed off; frontend-only create-gating fix; no migration/RPC/RLS/API change; Off-Page Authority module still NOT LOCKED (Stage 6 final regression next) |
| `STAGE_6_FINAL_REGRESSION_SIGNOFF.md` | Final regression + lock-readiness assessment for the approved Stage 6 scope (Off-Page Authority opportunity + campaign workflows; AI Visibility read-only): scope + explicit deferrals, static/SQL/authenticated-browser/mock-mode/earlier-stage results, role matrix, opportunity + campaign workflow regression, client create-gating regression, AI Visibility read regression + `manual_seed` source, data integrity, known benign observations, remaining risks, production-untouched, files changed. Concludes **READY FOR A SEPARATE MODULE-LOCK DECISION** (that decision has since been recorded — the implemented Stage 6 scope is now LOCKED in `MODULE_LOCKS.md`; deferred work remains unlocked). | 6 (Stage 6 regression) | **new** — regression PASS; the lock evidence for the 2026-07-13 Stage 6 implemented-scope lock; no app/migration/RPC/RLS/API change |
| `MVP_RELEASE_READINESS_AND_NEXT_SCOPE.md` | Planning/evidence doc: current stage (workflow/demo MVP vs customer-usable vs production-ready), locked foundation, full route inventory + data sources, deferred workflow-write assessment (campaign task-completion, AI Visibility writes), live-data roadmap (crawler → GSC → GA4/LLM) with sequencing, operational release gaps, strict marketing/customer-value test, P0–P3 dependency-ordered roadmap, release Options A/B/C, recommendation (**Option B**), exact next milestone (Website Crawler Phase 1), explicit deferred scope, known unknowns, and operator decision gates. | Cross-stage (release planning) | **new** — planning only; no code/migration/DB/prod change; recommends Option B + crawler-first next milestone |
| `ADR_CUSTOMER_AUTHENTICATION_FOR_MVP.md` | Architecture decision (Phase 16A): customer auth + route protection. Decision = **Option C hybrid** (standalone Supabase Auth for MVP + future parent-identity adapter seam); parent-SSO rejected for MVP (unconfirmed contract). Includes the full route-protection behavioural contract (auth/no-module-access/no-workspace/no-active-website/loading/redirect/deep-link/mock-mode/admin-preview) with RLS remaining authoritative. | 16A (architecture) | **new** — proposed; awaiting approval; no implementation |
| `ADR_CRAWLER_RUNTIME_ARCHITECTURE.md` | Architecture decision (Phase 16A): crawler runtime. Decision = **Option C hybrid** (guarded enqueue RPC/thin Edge Function control plane + dedicated service-role background worker; RLS-scoped reads; no BFF). Includes SSRF/robots/budget security, existing-table classification + additive `seo_crawl_jobs`, crawl lifecycle layer-ownership, failure/retry model, subscription/usage (schema-only today), and locked-module boundaries. | 16A (architecture) | **new** — proposed; awaiting approval; no implementation |
| `CRAWLER_PHASE_1_IMPLEMENTATION_PLAN.md` | Dependency-ordered crawler Phase-1 build plan (12 phases: auth+route-protection → job contract → guarded enqueue → worker → URL safety → discovery → extraction → issues → audit/inventory integration → frontend status → verification → GSC next), each with objective/layer/files/DB/API/frontend/permission/backward-compat/tests/migration/rollback/locked-modules; exact next task = customer auth + route protection. | 16A (architecture) | **new** — planning only; awaiting approval; no implementation |
| `PHASE_16B_CUSTOMER_AUTH_ROUTE_PROTECTION_SIGNOFF.md` | Sign-off for customer authentication + `/seo/*` route protection (login-only): approved hybrid architecture, route classification (public/dev-only/setup/website-scoped/admin), centralized `useSeoAccess()` session-resolution design, module-access + workspace/website prerequisite behaviour, safe deep-link restoration, customer sign-out + cross-user cleanup, mock-mode bypass, `seo_is_global_admin`-gated admin-preview, static + browser tests (owner/client/anonymous/admin-preview/mock), known limitations (global-admin-allow + setup states code-covered not live-tested; no unit framework), files changed, no DB/API change, backward compatibility, production untouched. | 16B (auth) | **new** — implemented + TEST-validated; RLS-authoritative; no migration/RPC/RLS change; auth NOT module-locked; next = Crawler Phase 1 |
| `CRAWLER_PHASE_1A_DATA_CONTRACT.md` | Crawler job control-plane DB contract (Phase 16C): final schema (`seo_crawl_jobs`/`seo_crawl_attempts`/`seo_crawl_events`), status model, role matrix, request/cancel/worker-claim RPC contracts, default-deny RLS + diagnostics isolation, idempotency + active-job dedup + `FOR UPDATE SKIP LOCKED` claim, lease/retry/cancellation model, deferred usage/plan enforcement + external ownership verification, index purposes, security, backward compatibility, migration order, verification results, rollback, deferred Phase 1B worker scope. | 16C (crawler contract) | **new** — implemented + TEST-verified (ALL PASS); additive migration `20260713120025`; no worker/crawling; no existing object altered |
| `supabase/migrations/20260713120025_seo_phase16c_crawl_control_plane.sql` | Additive crawler control-plane migration (tables + constraints + indexes + RLS + guarded request/cancel RPCs + service-role-only worker-claim). Applied to `Digi_SEO_Test`. | 16C | **new** — additive; no existing object altered |
| `supabase/test/seo_phase16c_crawl_control_plane_verification.sql` | TEST-only verification for the crawler control plane — structure, enqueue role/eligibility/dedup, direct-write RLS denial, cancellation, worker-claim atomicity/retry/cancellation, data isolation; idempotent + self-cleaning (`PHASE16C-VERIFY-` tag). | 16C | **new** (ALL PASS on TEST) |
| `supabase/test/seo_phase16c_rollback_TEST_ONLY.sql` | Documented reverse-order rollback for migration `20260713120025` (drop RPCs → helper/trigger fn → tables). TEST-only; do not execute unless instructed. | 16C | **new** — rollback reference |
| `CRAWLER_PHASE_1B_WORKER_SKELETON.md` | Crawler worker skeleton + secure job-lifecycle sign-off (Phase 16D): Node/TS runtime decision, `crawler-worker/` structure, secure config + redaction, claim contract, **lease-token** ownership model, service-role-only lifecycle functions, heartbeat/retry/cancellation/stale-recovery, secret-safe logging + graceful shutdown, non-root container, unit tests, DB verification, TEST integration evidence (no crawling; no page/audit writes), security checks, backward compatibility, rollback, Phase 1C boundary. | 16D (crawler worker) | **new** — implemented + TEST-verified; no crawling; not deployed |
| `supabase/migrations/20260714120026_seo_phase16d_worker_lifecycle.sql` | Additive worker-lifecycle migration: `lease_token` columns (jobs + attempts), enhanced claim, and service-role-only worker functions (heartbeat/complete/partial/fail/schedule_retry/acknowledge_cancellation, recover_stale_jobs) + ownership guard. Applied to `Digi_SEO_Test`. | 16D | **new** — additive; no existing status value changed |
| `supabase/test/seo_phase16d_worker_lifecycle_verification.sql` | TEST-only verification for the worker lifecycle — structure/grants, happy path, lease-token mismatch, stale recovery + reassignment invalidation, retry + max-attempts, cancellation ack, RLS unchanged; idempotent + self-cleaning. | 16D | **new** (ALL PASS on TEST) |
| `supabase/test/seo_phase16d_rollback_TEST_ONLY.sql` | Reverse-order rollback for migration `20260714120026` (drop worker fns + guard → restore pre-16D claim → drop `lease_token` columns; preserves Phase 16C). TEST-only; do not execute unless instructed. | 16D | **new** — rollback reference |
| `crawler-worker/` | Node/TS dedicated crawler worker (outside `src/`): config/logger/errors/util/supabaseClient/jobGateway/processor/worker/index + `src/discovery/*` (Phase 1C) + tests + Dockerfile + `.env.example`. Phase 1B lifecycle + Phase 1C discovery (SSRF-safe fetch, robots, sitemap, HTML link discovery, budgets); **no SEO analysis; no page-inventory/audit writes; not deployed**. | 16D–16E | **updated** — discovery engine added |
| `CRAWLER_PHASE_1C_DISCOVERY_ENGINE.md` | Crawler discovery-engine sign-off (Phase 16E): scope/exclusions, URL + IP safety, SSRF + connection-time DNS-rebinding controls, allowed-origin policy, safe HTTP client, user-agent, robots (RFC 9309), XXE-safe sitemap parsing, HTML link discovery, normalization, queue/budgets, persistence schema + RLS + worker RPCs, job-outcome semantics, packages added, unit + DB + integration evidence, security review, real-public-network test status (not live-tested), rollback, Phase 1D boundary. | 16E (crawler discovery) | **new** — implemented + TEST-verified; no SEO analysis; crawler not customer-available |
| `supabase/migrations/20260714120027_seo_phase16e_crawl_discovery.sql` | Additive discovery-storage migration: `seo_crawl_discovered_pages` + `seo_crawl_sitemaps` (customer-read RLS; no customer writes) + `discovery_stats` column + service-role-only `seo_crawl_worker_record_discovery` / `update_discovery_progress`. Applied to `Digi_SEO_Test`. | 16E | **new** — additive; no existing status/RPC changed |
| `supabase/test/seo_phase16e_crawl_discovery_verification.sql` | TEST-only verification for discovery storage — structure/grants, worker-only record + progress, lease-token mismatch, customer direct-write denial, member read + non-member isolation, upsert dedupe; idempotent + self-cleaning. | 16E | **new** (ALL PASS on TEST) |
| `supabase/test/seo_phase16e_rollback_TEST_ONLY.sql` | Reverse-order rollback for migration `20260714120027` (drop discovery RPCs → drop tables → drop `discovery_stats`; preserves 16C/16D). TEST-only; do not execute unless instructed. | 16E | **new** — rollback reference |
| `CRAWLER_PHASE_1D_EXTRACTION_AND_ISSUE_DETECTION.md` | Crawler extraction + issue-detection sign-off (Phase 16F): scope/exclusions, no-second-fetch extraction architecture, charset handling, text normalization, canonical + robots directives, extracted fact list + data minimization, versioned issue-rule registry + full stable code list, thresholds, duplicate detection, severity mapping, persistence schema + RLS + worker RPCs, outcome semantics, unit/DB/integration + security evidence, dependencies (none), rollback, Phase 1E boundary. | 16F (crawler extraction) | **new** — implemented + TEST-verified; no audit/page-inventory publishing; crawler not customer-available |
| `supabase/migrations/20260714120028_seo_phase16f_crawl_extraction.sql` | Additive extraction migration: `seo_crawl_page_snapshots` + `seo_crawl_issues` (customer-read RLS; no customer writes; data-minimized) + `extraction_stats` + service-role-only `seo_crawl_worker_record_snapshots`/`record_issues`/`update_extraction_progress`. Applied to `Digi_SEO_Test`. | 16F | **new** — additive; no existing status/RPC changed; no locked-table writes |
| `supabase/test/seo_phase16f_crawl_extraction_verification.sql` | TEST-only verification: structure/grants, worker-only snapshot/issue/progress, lease-token mismatch, idempotent upserts, unknown-code + oversized-evidence + scope-integrity rejection, member read + non-member isolation + customer direct-write denial; self-cleaning. | 16F | **new** (ALL PASS on TEST) |
| `supabase/test/seo_phase16f_rollback_TEST_ONLY.sql` | Reverse-order rollback for migration `20260714120028` (drop extraction RPCs → issues → snapshots → `extraction_stats`; preserves 16C/16D/16E). TEST-only; do not execute unless instructed. | 16F | **new** — rollback reference |
| `CRAWLER_PHASE_1E_PAGE_INVENTORY_AUDIT_PUBLISHING.md` | Crawler publishing sign-off (Phase 16G): scope/exclusions, explicit crawl-job→audit-run association (+ rejected latest-run/alternatives), publication VERSION 1, Page-Inventory mapping (identity/ownership/staleness), complete 29-code Audit-Issue mapping + severity map, site-issue representation, provenance, idempotency, stale-job protection, partial handling, transactional publish RPC, RLS/grants, worker integration, unit/DB/E2E + browser-note + security evidence, backward compatibility, known limits (no recs/UI/prod), rollback, Phase 1F boundary. | 16G (crawler publishing) | **new** — implemented + TEST-verified; no recommendations / customer UI; crawler not customer-available |
| `supabase/migrations/20260714120029_seo_phase16g_publishing.sql` | Additive publishing migration: `seo_crawl_jobs.audit_run_id` + guarded `seo_crawl_request_audit` (existing request/run RPCs unchanged); 29-code `seo_crawl_issue_audit_map`; nullable provenance on `seo_page_inventory` + `seo_audit_issues`; `seo_crawl_publications`; service-role-only transactional `seo_crawl_worker_publish_results`. Applied to `Digi_SEO_Test`. | 16G | **new** — additive; no existing status/RPC/constraint changed; no `seo_recommendations`/locked-table writes |
| `supabase/test/seo_phase16g_publishing_verification.sql` | TEST-only verification: structure/grants/29-code map; explicit association (+ cross-workspace + terminal-job denial; no latest-run guess); publish authz (stale-lease/cancelled/wrong-state); Page-Inventory mapped fields + idempotency + newer-vs-older stale protection + manual/user-owned preservation + seed isolation + no Page-Performance write; Audit-Issue mapping/severity/scope/provenance + idempotency + manual preservation + sibling-run untouched + no Recommendation write + run completed/count/score; transactional rollback; RLS member/customer/non-member; self-cleaning. | 16G | **new** (ALL PASS on TEST) |
| `supabase/test/seo_phase16g_rollback_TEST_ONLY.sql` | Reverse-order rollback for migration `20260714120029` (drop publish RPC → orchestration RPC → `seo_crawl_publications` → `seo_crawl_issue_audit_map` → provenance columns → `audit_run_id`; preserves 16C–16F). TEST-only; do not execute unless instructed. | 16G | **new** — rollback reference |
| `PHASE_16H_CRAWLER_CUSTOMER_UI_SIGNOFF.md` | Customer crawl UI sign-off (Phase 16H): scope, journey, role matrix, status model + Supabase-only polling, freshness, cancellation, partial handling, published-result integration, mock behaviour, tests, operator acceptance (all 7 scenarios PASS incl. 6A/6B/6C lifecycle + Scenario 7 isolation), security/permissions, backward compatibility, files, rollback, next milestone. **No DB change.** | 16H (crawler customer UI) | **updated** — **FULLY ACCEPTED ON TEST — PRODUCTION READINESS NOT STARTED** (all 7 scenarios PASS) |
| `OPERATOR_USER_ACCEPTANCE_TEST_GUIDE.md` | Operator UAT guide for the crawler customer UI: env startup (frontend + secret-safe worker one-shot), roles-not-passwords, 7 scenarios (owner/team/client/cancel/partial/failure/refresh-signout), cleanup, evidence template. | 16H | operator guide |
| `OPERATOR_TEST_RESULTS.md` | Per-scenario operator results (state `OPERATOR ACCEPTED — COMPLETE WITH ADMINISTRATIVE NOTES`): Scenarios 1–5 + 7 operator browser results; 6A/6B/6C controlled DB-level lifecycle evidence; three low-risk Scenario 7 artifacts noted as un-saved (documentation-only). | 16H | **updated** — all scenarios PASS |
| `supabase/migrations/20260715120030_seo_crawl_audit_finalization.sql` | Additive Phase 16H defect fix: finalizes a linked **running** audit to `failed` when its crawl is cancelled/failed/exhausted (never overwrites completed history), via `_seo_crawl_finalize_linked_audit_failed` invoked by cancel/fail/schedule_retry(exhaust)/stale-recovery. Applied to `Digi_SEO_Test`. | 16H | **new** — additive; no existing status/RPC signature changed |
| `supabase/test/seo_phase16h_crawl_audit_finalization_verification.sql` | TEST-only verification: queued/active cancellation finalization; direct worker failure; retry-wait stays running vs exhausted→failed; stale-recovery retryable vs exhausted; completed-audit history preserved. | 16H | **new** (ALL PASS on TEST) |
| `src/pages/seo/audit/crawl/*`, `src/hooks/useWebsiteCrawl.ts`, `src/services/crawlService.ts` (+ `supabase/seoCrawlSupabaseService.ts`, `mocks/crawlMockData.ts`), `src/lib/crawlStatus.ts`, `src/types/crawl.ts` | Phase 16H frontend crawl workflow (request/status/freshness/cancel/results on `/seo/audit`). Additive; no DB change; customer-safe reads; Supabase-only status; mock preview. | 16H | **new** — frontend only; no locked file changed |
| `P1A_STEP1_OWNERSHIP_VERIFICATION_DB_CONTRACT.md` | P1a Step 1 implementation record — Domain Ownership Verification DATABASE CONTRACT (DNS-TXT): the two additive tables + RLS + integrity trigger, design invariants, exclusions (no RPC/DNS/worker/frontend; P1b excluded), locked-scope compliance, TEST verification evidence, rollback, next step. | P1a Step 1 | **new** — implemented + TEST-verified on `Digi_SEO_Test`; additive; production untouched |
| `supabase/migrations/20260716120031_seo_p1a_step1_ownership_verification.sql` | Additive P1a Step 1 migration: `seo_ownership_verifications` + append-only `seo_ownership_verification_events` (customer-safe; DNS-TXT only; default-deny-write RLS; workspace/website integrity trigger; provenance seam). Applied to `Digi_SEO_Test`. No RPC/crawl/locked-table change. | P1a Step 1 | **new** — additive; no existing object altered |
| `supabase/test/seo_p1a_step1_ownership_verification_verification.sql` | TEST-only verification: structure/RLS/policy-shape, privileged fixture create, duplicate + integrity + FK + unknown-status rejection, member vs non-member/cross-workspace reads, authenticated direct-write denial, audit immutability, other-module isolation; idempotent + self-cleaning (`OWNVERIFY-` tag). | P1a Step 1 | **new** (ALL PASS on TEST 2026-07-16) |
| `supabase/test/seo_p1a_step1_ownership_verification_rollback_TEST_ONLY.sql` | Reverse-order rollback for migration `20260716120031` (triggers/integrity fn → audit table → verifications table; preserves shared `set_updated_at()`). TEST-only; do not execute unless instructed. | P1a Step 1 | **new** — rollback reference |
| `P1A_STEP2A_OWNERSHIP_VERIFICATION_RPCS.md` | P1a Step 2A implementation record — the 4 guarded **customer** ownership-verification RPCs (initiate/recheck/reverify/revoke) + 3 internal helpers: exact behavioural contract, owner/admin permission matrix, no-status-RPC (direct RLS) decision, security, exclusions (no service-role/override/worker/DNS/frontend; Step 2B + P1b excluded), TEST verification evidence, rollback, next step. | P1a Step 2A | **new** — implemented + TEST-verified on `Digi_SEO_Test`; additive; production untouched |
| `supabase/migrations/20260716120032_seo_p1a_step2a_ownership_verification_rpcs.sql` | Additive P1a Step 2A migration: 4 SECURITY DEFINER `authenticated`-only customer RPCs (owner/admin-gated) over the Step-1 tables + 3 internal helpers (host-parse, strong token, shared authz). No table/policy change; no crawl/locked-table change; no status RPC. Applied to `Digi_SEO_Test`. | P1a Step 2A | **new** — additive; no existing object altered |
| `supabase/test/seo_p1a_step2a_ownership_verification_rpcs_verification.sql` | TEST-only verification (23 checks): signatures/SECDEF/search_path/grants; owner+admin allowed; team/client/non-member/anon + cross-workspace denied; initiate create+idempotency; recheck token-reuse; reverify rotation; revoke+idempotent revoke; admin restart rotation; one-event-per-meaningful-change; direct-write denial; customer-safe RLS reads; internal-field guard; crawler RPC non-regression; other-module isolation; self-cleaning. | P1a Step 2A | **new** (ALL PASS on TEST 2026-07-16) |
| `supabase/test/seo_p1a_step2a_ownership_verification_rpcs_rollback_TEST_ONLY.sql` | Reverse-order rollback for migration `20260716120032` (drops only the 4 RPCs + 3 helpers; preserves Step-1 tables/history, crawler, Page Performance, Stage 6, shared `set_updated_at()`). TEST-only; do not execute unless instructed. | P1a Step 2A | **new** — rollback reference |
| `P1A_STEP2B_OWNERSHIP_VERIFICATION_SERVICE_RPCS.md` | P1a Step 2B implementation record — the internal claim/lease ledger + 3 RPCs (service-role claim, service-role result-persistence, authenticated global-admin override): lease model, exact contracts, security, no-lock-update rationale, TEST verification evidence, rollback, next step (Step 3 worker). | P1a Step 2B | **new** — implemented + TEST-verified on `Digi_SEO_Test`; additive; production untouched |
| `supabase/migrations/20260716120033_seo_p1a_step2b_ownership_verification_service_rpcs.sql` | Additive P1a Step 2B migration: internal `seo_ownership_verification_claims` (global-admin-read-only; open-claim unique index) + `seo_ownership_verification_claim` / `record_result` (service_role only) + `admin_override` (authenticated, global-admin-gated). No crawler/Step-1/2A/locked-table change; no existing policy modified. Applied to `Digi_SEO_Test`. | P1a Step 2B | **new** — additive; no existing object altered |
| `supabase/test/seo_p1a_step2b_ownership_verification_service_rpcs_verification.sql` | TEST-only verification (35 checks): structure/grants (service_role-only claim/result; global-admin-gated override); claim eligibility/dedup/stale-recovery/returned-fields/cross-website; result verified/failed/idempotency/token-preservation/internal-diagnostics-hidden; global-admin allow (via a self-cleaning temporary `profiles` stub) + non-admin denial; Step 2A/RLS/crawler non-regression; other-module isolation; self-cleaning. | P1a Step 2B | **new** (ALL PASS on TEST 2026-07-16) |
| `supabase/test/seo_p1a_step2b_ownership_verification_service_rpcs_rollback_TEST_ONLY.sql` | Reverse-order rollback for migration `20260716120033` (drops only the 3 RPCs + the claims table; preserves Step-1/2A objects, crawler, Page Performance, Stage 6). TEST-only; do not execute unless instructed. | P1a Step 2B | **new** — rollback reference |
| `P1A_STEP3_OWNERSHIP_VERIFICATION_WORKER.md` | P1a Step 3 implementation record — the isolated DNS-TXT verification worker module (runner + gateway + resolver + `verify-once` mode): DNS contract, failure mapping, independence from the crawler, security/logging, verification evidence, TEST integration proof, code-only rollback, next step (frontend Step 4). Approved additive extension of the locked `crawler-worker/**`. | P1a Step 3 | **new** — implemented + TEST-verified; code-only (no migration/RPC); production untouched |
| `crawler-worker/src/verification/{dns,verificationGateway,runner}.ts`, `crawler-worker/src/modes.ts`, `crawler-worker/test/ownershipVerification.test.ts` | P1a Step 3 isolated ownership-verification worker module (DNS-TXT resolver + Step 2B RPC gateway + one-shot runner + `verify-once` mode). Reuses the service-role client/logger/config/shutdown; imports nothing from the crawl processor. Additive edits: `crawler-worker/src/index.ts` (`verify-once` branch), `crawler-worker/src/config.ts` (2 optional fields). | P1a Step 3 | **new** — worker code; no DB change; no crawler contract change |
| `supabase/test/seo_p1a_step3_worker_dns_verification_integration.sql` | TEST-only integration proof: the worker's claim→result→audit flow via the **real** Step 2B RPCs with a deterministic simulated resolver (match→verified, not-found→failed); asserts token-unchanged, audit events, internal-diagnostics-hidden, and 0 crawl/audit/Page-Inventory/Page-Performance/recommendation/Stage-6 rows changed; self-cleaning. | P1a Step 3 | **new** (ALL PASS on TEST 2026-07-16) |
| `P1A_STEP4_OWNERSHIP_VERIFICATION_FRONTEND_SERVICE.md` | P1a Step 4 implementation record — the frontend ownership-verification service layer (types, pure helpers, Supabase read/write service, public dispatcher, deterministic mock, hooks): customer-safe data contract, read/write behaviour, non-masking writes, mock lifecycle, query-key scoping, no-UI scope, verification evidence, code-only rollback, next step (Step 5 UI). | P1a Step 4 | **new** — implemented + type/build-verified; no UI; no DB change; production untouched |
| `src/types/ownershipVerification.ts`, `src/lib/ownershipVerification.ts`, `src/services/ownershipVerificationService.ts`, `src/services/supabase/seoOwnershipVerificationSupabaseService.ts`, `src/mocks/ownershipVerificationMockData.ts`, `src/hooks/useOwnershipVerification.ts` | P1a Step 4 frontend ownership-verification service layer (behind the mock/Supabase adapter; RLS read + Step 2A RPC writes; non-masking write helper; deterministic mock; user+website-scoped hooks). Additive edit: `src/services/supabase/supabaseTypes.ts` (customer-safe table + 4 Step 2A RPC constants). **No UI; no crawl/worker/DB change.** | P1a Step 4 | **new** — frontend service only; no locked file changed |
| `P1A_STEP5_OWNERSHIP_VERIFICATION_UI.md` | P1a Step 5 implementation record — the Websites/onboarding Domain Ownership Verification UI (`OwnershipVerificationPanel` in `WebsiteCard`): states/actions, permissions + accessibility, mock mode, verification evidence (incl. mock-mode + authenticated browser validation), code-only rollback. | P1a Step 5 | implemented + type/build + mock-browser-verified; **authenticated operator Step 2.8 (double-submit) ACCEPTED — PASS (2026-07-17)**; **authenticated browser role matrix ACCEPTED — PASS (2026-07-18)**; **real worker-binary run ACCEPTED — PASS (2026-07-19)**; P1a COMPLETE and MODULE-LOCKED; no DB change; production untouched |
| `src/pages/seo/websites/OwnershipVerificationPanel.tsx` (+ additive `src/pages/seo/WebsiteCard.tsx` integration) | P1a Step 5 customer-facing ownership-verification UI panel rendered inside the website card. Uses only the Step 4 hooks; owner/admin actions + read-only for others; DNS-TXT instructions + copy; accessible role tooltip (local, not the locked Stage 6 one); mock preview. **No crawl/worker/DB/RPC change; no new route.** | P1a Step 5 | **new** — UI only; no locked file changed |
| `P1A_STEP5_DOUBLE_SUBMIT_FIX.md` | P1a Step 5 defect-fix record — operator Step 2.8 double-submit. **§9 is authoritative: FINAL fix = per-action visible bounded post-action lock (`idle → in_flight → cooldown → idle`; synchronous `phaseRef` + `lockedActions`; first click immediate; disabled through mutation + refetch + fixed 3000 ms cooldown; no keep-alive). §§3/6a/8 record the two superseded interim guards (synchronous latch, then leading-edge throttle) — history only.** Includes the retired-criterion rationale (the "one RPC across an arbitrarily long burst" criterion was unsound; earlier long-burst runs = invalid tests, not failures), AT-1..AT-4 accepted criteria, mock quantitative proof, and §10 A3 DB proof + pending-record cleanup. | P1a Step 5 fix | UI-only; no DB/RPC/locked change; **Step 2.8 = PASS (2026-07-17); A3 proof + cleanup COMPLETE** |
| `P1A_DOMAIN_OWNERSHIP_VERIFICATION_SIGNOFF.md` | P1a Step 6 sign-off — implemented scope, exclusions (P1b not implemented), authenticated role matrix (backend PROVEN via SQL; UI browser PASS), worker integration (integration SQL PROVEN; real binary PASS), DB/security evidence, full regression results, known limitations (psql-style smoke runner incompatibility), production untouched. **Verdict: `P1A COMPLETE — MODULE-LOCKED` (2026-07-19).** §10 acceptance log records **Step 2.8 = PASS (2026-07-17)**, **A3 DB proof + pending-record cleanup COMPLETE (2026-07-17)**, **authenticated browser role matrix = PASS (2026-07-18)**, and **real `verify-once` worker-binary run = PASS (2026-07-19)** — all operator-acceptance items satisfied. | P1a Step 6 | validation + sign-off; no code/DB change; **P1a lock added to `MODULE_LOCKS.md` (2026-07-19); P1b is now the next stage** |
| `P1B_VERIFIED_ONLY_CRAWL_ENQUEUE_PLAN.md` | **Authoritative P1b implementation plan** — Verified-only Crawl Enqueue Enforcement. Design-spec only (no executed SQL). Enforcement boundary (`seo_crawl_request`, after role-authz, before eligibility/INSERT), ownership source of truth (`seo_ownership_verifications` `dns_txt`/`verified`), `FOR SHARE` concurrency rationale, plain `P0001` error convention, additive `CREATE OR REPLACE` migration strategy (16C migration not edited), enqueue-path inventory (single boundary; worker never enqueues), new-enqueue-only scope, the six 16C–16H regression-fixture updates + nuances, the new P1b acceptance-script matrix, optional UI defense-in-depth, security/workspace-isolation, locked-contract/unlock procedure, and the implementation sequence. | P1b | **updated** (2026-07-19) — TEST-APPLIED, VERIFIED, and MODULE-LOCKED (see sign-off + rows below); production untouched |
| `supabase/migrations/20260719120034_seo_p1b_verified_only_crawl_enqueue.sql` | **P1b migration (APPLIED to TEST)** — `CREATE OR REPLACE FUNCTION public.seo_crawl_request(...)` adding the verified-ownership guard (`FOR SHARE` on `seo_ownership_verifications` `dns_txt`/`verified`; plain `P0001` message) after role-authz and before eligibility/INSERT. Deployed contract verified unchanged except the guard; `seo_crawl_request_audit`, worker, and all other contracts unchanged; applied 16C migration NOT edited. | P1b | **applied to `Digi_SEO_Test` 2026-07-19** (recorded once); immutable; production untouched |
| `supabase/test/seo_p1b_verified_only_crawl_enqueue_rollback_TEST_ONLY.sql` | TEST-only rollback — `CREATE OR REPLACE seo_crawl_request` back to the exact pre-P1b (Phase 16C) body (verified byte-identical). Drops nothing; P1a untouched. **Not used** (P1b passed). | P1b | **new** — rollback reference; TEST-only |
| `supabase/test/seo_p1b_verified_only_crawl_enqueue_verification.sql` | P1b acceptance verification — verified success (direct + audit), pending/failed/revoked/missing blocked, authz/authn precedence (anon/client/non-member/cross-workspace fail first, no ownership-state leak), idempotency + single-active-job + one-queued-event compatibility, no-orphan-audit-run on a rejected orchestration, and a static FOR-SHARE/guard-presence assertion; idempotent + self-cleaning. | P1b | **ALL PASS on TEST 2026-07-19** |
| `P1B_CONCURRENCY_VERIFICATION_GUIDE.md` | Two-session (Session A/B) guide proving the `FOR SHARE` write-time behaviour (enqueue-wins vs revoke-wins) — the single-transaction `supabase db query -f` runner cannot execute a live race. TEST-only; no secrets. Includes the executed 2026-07-19 evidence. | P1b | **executed — PASS (2026-07-19)**; both scenarios |
| `P1B_VERIFIED_ONLY_CRAWL_ENQUEUE_SIGNOFF.md` | **P1b sign-off** — verdict `P1b COMPLETE — TEST-APPLIED, VERIFIED, MODULE-LOCKED` (2026-07-19): migration applied, deployed-RPC contract check, P1b acceptance ALL PASS, 16C–16H regression ALL PASS, worker 74/74, live two-session concurrency PASS, cleanup/safety, lock status, production untouched. | P1b | **new** (2026-07-19) — sign-off; P1b LOCKED |
| `DIGIBILITY_SEO_HELP_CENTER_PRODUCT_SPEC.md`, `DIGIBILITY_SEO_HELP_CENTER_INFORMATION_ARCHITECTURE.md`, `DIGIBILITY_SEO_CONTEXTUAL_HELP_MAPPING.md`, `DIGIBILITY_SEO_HELP_CENTER_IMPLEMENTATION_PLAN.md` | Help Center Phase 1 planning specification (business objectives, IA, contextual-help mapping for a future wave, implementation-wave plan). Read-only/planning deliverables that preceded Slice 1 implementation. | Help Center | **draft spec** (2026-07-19) — planning only; superseded for Slice 1 scope by the Slice 1 implementation record below |
| `DIGIBILITY_SEO_END_TO_END_USER_JOURNEY_AND_SUPPORT_BLUEPRINT.md`, `DIGIBILITY_SEO_SUPPORT_CONTENT_PRODUCTION_MATRIX.md` | End-to-end user journey and support-content production blueprint underlying the Help Center content strategy. Draft; informs content authored in Slice 1. | Help Center | **draft** (2026-07-19) — planning reference |
| `DIGIBILITY_SEO_HELP_CENTER_SLICE1_PUBLIC_FOUNDATION.md` | **Help Center Slice 1 implementation record** — public, authentication-free, Supabase-independent documentation surface (`/help*` routes, `src/help/**` content+search+validation, `src/pages/help/**` UI), architecture/auth-independence proof, 30-article starter corpus (29 public + 1 internal), hand-rolled search design (incl. the stopword/match-cap ranking fix), content-safety/prohibited-pattern validation (incl. the `service_role` regex narrowing fix), accessibility, static-hosting open item, verification evidence, known limitations, and rollback instructions. | Help Center Slice 1 | **new** (2026-07-19) — IMPLEMENTED AND VERIFIED; frontend-only; no locked module touched; no DB/backend/worker/production change |
| `src/help/**` (types, categories, synonyms, search, validate, content/*) | Help Center Slice 1 content/search/validation layer — bundled TypeScript article corpus, `publicArticles()` visibility chokepoint, hand-rolled client-side search, pure content-integrity validator. No Supabase/auth dependency. | Help Center Slice 1 | **new** — frontend-only; no locked file changed |
| `src/pages/help/**` | Help Center Slice 1 public UI — shell, home/search/category/article/not-found pages, dev-only content-check page, shared presentational components. Registered as public route siblings in `src/routes/SeoRoutes.tsx`, outside `ProtectedRoute`. Additive touches: `src/components/layout/Sidebar.tsx` (nav link), `src/pages/seo/ExpertSupportPage.tsx` (cross-link paragraph). | Help Center Slice 1 | **new** — frontend-only; no locked file changed |
| `DIGIBILITY_SEO_HELP_CENTER_SLICE1A_COMPLETION.md` | **Help Center Slice 1A completion record** — the new Decline Diagnosis article, search-coverage additions (9 new fixtures, no shared-synonym change), full 14-fixture regression re-run (zero regressions, 1 improved), deployment-readiness findings (no target identifiable, documented only), UX review (2 small fixes: audience-label humanization, `relatedLink` block spacing), accessibility review, verification results, known limitations, Wave-2 readiness. | Help Center Slice 1A | **new** (2026-07-19) — IMPLEMENTED AND VERIFIED; frontend-only; no locked module touched; no DB/backend/worker/production change |
| `src/help/content/decline.ts`, additive edits to `src/help/categories.ts` (`reports-decline-diagnosis` category) and `src/help/content/index.ts` (wiring) | Help Center Slice 1A content addition — one new public article (`investigating-traffic-ranking-decline`) closing Slice 1's documented Decline Diagnosis content-coverage gap; grounded in the real `DeclineCause`/Page-Performance data model; possible/likely/confirmed framing throughout; no invented search-engine ranking behavior. | Help Center Slice 1A | **new** — content-only; no locked file changed |
| `src/pages/help/HelpArticlePage.tsx` (audience-role label humanization), `src/pages/help/components/BodyRenderer.tsx` (`relatedLink` block-display fix) | Two small, pre-existing UI defects found during Slice 1A's UX review and fixed: raw `audienceRoles` enum values now humanized (matches the adjacent content-type field); consecutive `relatedLink` body blocks now get visible vertical separation (were inline, so `space-y-4` had no visual effect). Both one-line, additive, no redesign. | Help Center Slice 1A | **fixed** (2026-07-19) — cosmetic only; no behavior/data-model change |
| `DIGIBILITY_FRONTEND_CLOUD_RUN_DEPLOYMENT_READINESS.md` | **Cloud Run deployment-readiness record** — chosen container architecture (multi-stage Docker: `node:20.18-alpine` build → `nginxinc/nginx-unprivileged:1.27-alpine` runtime), `$PORT` handling, SPA fallback + missing-asset 404 routing design, cache/security-header policy, environment-variable strategy (build-time-baked `VITE_*`, not Cloud Run runtime vars), local build/run commands, static verification results (Docker unavailable in this environment — no container build/run was performed or claimed), required-but-undecided Cloud Run service settings, rollback, known limitations, and the exact operator deployment checklist. | Cloud Run readiness | **new** (2026-07-19) — LOCAL READINESS ESTABLISHED; NOT DEPLOYED; no GCP resource created/contacted; no locked module/backend/worker/DB/Supabase-config change |
| `Dockerfile`, `.dockerignore`, `docker/nginx.conf.template`, `docker/security-headers.conf` | Repo-root container artifacts for serving the compiled Vite/React SEO frontend on Cloud Run: multi-stage build (unmodified `npm ci`/`npm run build`), non-root nginx runtime, `${PORT}`-templated server config with correct SPA-fallback vs. real-404 routing precedence, immutable long-lived caching for content-hashed assets, `no-cache` for `index.html`, and low-risk security headers only (no new CSP/HSTS). Excludes `node_modules`, `dist`, all `.env*` files, `crawler-worker/`, `supabase/`, and root documentation from the build context. | Cloud Run readiness | **new** — infrastructure-only; no application/backend/worker file changed; no image built or pushed |
| `DIGIBILITY_SEO_CONTEXTUAL_HELP_MAPPING.md` | Phase-1 draft contextual-help placement map across every `/seo/*` route (UX-pattern list + lock-impact table). Predates the real implemented Help Center — its route shape (`/seo/help/article/:slug`) and article codes (`A07`, `N03`, etc.) are stale/superseded; used only as a structural reference for the later read-only Wave 2A audit and the Wave 2B implementation, never as a source of valid slugs or routes. | Help Center Wave 2 | **draft** (superseded route/slug references) — planning reference only |
| `DIGIBILITY_SEO_HELP_CENTER_WAVE2B_FIRST_CONTEXTUAL_LINKS.md` | **Wave 2B implementation record** — the first approved batch of 4 contextual-help placements (Websites, Business Onboarding, Dashboard zero-website empty state, Login both branches), the new `src/help/routes.ts` route-constant design, exact insertion points, article mappings, accessibility behaviour (incl. the disclosed synthetic-focus tooling limitation), full verification results (incl. the disclosed unreconciled viewport-measurement anomaly), lock/backward-compatibility confirmation, remaining Wave 2C candidates, and the still-deferred Cloud Run runtime-verification status. | Help Center Wave 2B | **new** (2026-07-19) — IMPLEMENTED AND VERIFIED; frontend-only; no locked module touched; no backend/DB/routing/worker/Supabase-config change |
| `src/help/routes.ts` | New minimal Help Center route-constant module — `HELP_ROUTES`, exactly 4 entries used by this batch, no generic URL builder, no React component, no internal-article route, no Help Center routing change. | Help Center Wave 2B | **new** — frontend-only; no locked file changed |
| `src/pages/seo/WebsitesPage.tsx`, `src/pages/seo/BusinessOnboardingPage.tsx`, `src/pages/seo/SeoDashboardPage.tsx`, `src/pages/seo/SeoLoginPage.tsx` | Additive contextual-help `<Link>` insertions only (see the Wave 2B record for exact placement per file). No form, mutation, validation, submit-handler, redirect/`returnTo`, or mock-mode logic changed in any of the four files — confirmed by direct diff review; none of the four files appear in any `MODULE_LOCKS.md` locked-file list. | Help Center Wave 2B | **changed** (2026-07-19) — additive only; no locked file; no logic change |
| `DIGIBILITY_SEO_HELP_CENTER_WAVE2B_FIRST_CONTEXTUAL_LINKS.md` §11 (additive update) | **Wave 2B.5 refinement note** — root-caused and fixed the Business Onboarding spacing inconsistency (nested-`<div>` vs. direct-`CardHeader`-child `space-y-1.5` inheritance), and added a 3-part placement guideline (header-level pages / empty-state pages / shared-complex pages) for Wave 2C. Explicitly documents that no tab order was changed and DOM-order-before-a-primary-action on header-level pages is not, by itself, a defect. | Help Center Wave 2B.5 | **updated** (2026-07-19) — additive section appended; §1–10 history preserved unchanged |
| `src/pages/seo/BusinessOnboardingPage.tsx` (spacing fix only) | One-class additive fix: the existing inner `<div>` wrapping the page title/description/help-link now carries `className="space-y-1.5"`, matching `CardHeader`'s own token so the help link inherits the same 6px vertical rhythm as `WebsitesPage.tsx`/`SeoLoginPage.tsx`. Link target/text/shared-class-string, form fields, validation, and submit/mutation logic all unchanged. | Help Center Wave 2B.5 | **changed** (2026-07-19) — additive; no locked file; no form/mutation change |
| `DIGIBILITY_SEO_HELP_CENTER_WAVE2C_UNLOCKED_CONTEXTUAL_LINKS.md` | **Wave 2C implementation record** — contextual-help links added to Page Optimizer, Decline Diagnosis, Progress Reports, Approval Queue, 4 `PlaceholderPage` call sites, and `RouteStates.tsx`'s `AccessRequiredState` (module variant only); the new `PlaceholderPage` optional-prop API; per-page article-fit rationale; 4 deferred candidates with reasons (`RoadmapSummaryHeader.tsx`, `CompetitorOverviewHeader.tsx`, `AuditHeader.tsx`, `RouteStates.tsx`'s `ResolutionErrorState`/admin variant); full verification results; lock confirmation; remaining Wave 3 scope. | Help Center Wave 2C | **new** (2026-07-19) — IMPLEMENTED AND VERIFIED; frontend-only; no locked module touched; no backend/DB/routing/auth-logic change |
| `src/help/routes.ts` (4 new entries) | `APPROVAL_WORKFLOW`, `DECLINE_DIAGNOSIS`, `DIGIBILITY_OPERATING_MODEL`, `FEATURE_AVAILABILITY` — all verified published+public before use; no unused constant, no URL builder, no routing change. | Help Center Wave 2C | **changed** — additive; no locked file |
| `src/pages/seo/PageOptimizerPage.tsx`, `src/pages/seo/DeclineDiagnosisPage.tsx`, `src/pages/seo/ReportsPage.tsx`, `src/pages/seo/ApprovalQueuePage.tsx` | Additive contextual-help `<Link>` insertions in each page's header, outside every selector/filter/mutation/role-switching control (see the Wave 2C record for exact placement + article-fit rationale per page). Decline Diagnosis and Approval Queue also received a `space-y-1.5` fix on their nested title/description `<div>`, applying the Wave 2B.5 lesson proactively. No query, mutation, filter, or role logic changed in any of the four files. | Help Center Wave 2C | **changed** (2026-07-19) — additive only; no locked file; no logic change |
| `src/pages/seo/PlaceholderPage.tsx` (new optional props), `src/pages/seo/BlogBriefsPage.tsx`, `src/pages/seo/KeywordResearchPage.tsx`, `src/pages/seo/ContentGapsPage.tsx`, `src/pages/seo/SeoSettingsPage.tsx` | New additive, backward-compatible `helpRoute`/`helpLabel` optional props on `PlaceholderPage.tsx` (omitted → renders exactly as before, verified live); wired into 4 call sites where `preview-data-versus-live-data` is a genuinely relevant match. `BusinessOnboardingPage.tsx`'s blocked-state call and the unreachable `ModulePlaceholderPage.tsx` were deliberately left unchanged. | Help Center Wave 2C | **changed** (2026-07-19) — additive only; no locked file |
| `src/components/auth/RouteStates.tsx` | One conditional contextual-help link added to `AccessRequiredState`'s module variant only (`{!isAdmin && (...)}`), pointing to `signing-in-and-access-states`. Purely presentational file (no auth-resolution logic); placement is inside `CardHeader`, separate from the `onRetry`/`onSignOut` buttons in `CardContent`; `ResolutionErrorState` and the admin variant are unchanged. | Help Center Wave 2C | **changed** (2026-07-19) — additive only; no locked file; no auth-logic change |
| `DIGIBILITY_SEO_HELP_CENTER_WAVE3_DEFERRED_SURFACES_AND_FINAL_SIGNOFF.md` | **Help Center Wave 3 sign-off + final module completion decision.** Zero-code-change sign-off wave: re-audited the four Wave-2C-deferred contextual-help candidates (Roadmap header, Competitor header, Audit header, RouteStates admin variant) against current code + locks and **closed all four without implementation**, each with evidence (no strong-fit published article; redundant with existing inline honesty; crawl-locked-domain overlap; no global-admin article). Includes the candidate decision matrix, per-candidate close rationale, verification results, lock-boundary re-check, a **"Future Expansion Triggers" section** (7 specific conditions under which the 4 closed candidates should be re-evaluated — not automatically re-implemented — plus the unchanged strong-fit/low-risk/lock-boundary bar and per-module approval requirement that still applies), and the **final decision that the Help Center is DEVELOPMENT-COMPLETE** — while explicitly leaving locked-module contextual links (Wave-3 locked scope) and Cloud Run runtime verification outside that decision. | Help Center Wave 3 | **updated** (2026-07-20) — COMPLETE; Future Expansion Triggers added (documentation-only, additive); zero code change; no locked file; no backend/DB/auth/route change |
| `DIGIBILITY_SEO_COLLAPSIBLE_NAVIGATION_INFORMATION_ARCHITECTURE.md` | **Sidebar navigation restructure record.** SEO as one collapsible top-level module (SEO Dashboard standalone first, then 7 collapsible logical groups in customer-journey order), replacing the previous flat 20-item list. Documents the full hierarchy, old-vs-new page ordering, the "Visibility Dashboard" → "SEO Dashboard" terminology decision (and the full SEO-vs-Visibility terminology audit table — what was renamed, retained internally, or correctly left untouched as unrelated legitimate "visibility" usage), desktop/collapsed-sidebar/mobile behavior (including the verified, pre-existing absence of any mobile nav drawer — not built, out of scope), active-route auto-expansion rules, localStorage persistence design, permission-filtering confirmation, accessibility behavior, backward-compatibility evidence, Help Center content reconciliation, verification results, known limitations, and future-module extensibility notes. | Navigation IA | **new** (2026-07-20) — IMPLEMENTED AND VERIFIED; no locked module touched; no backend/DB/route-path/auth/permission change |
| `src/registry/navigationGroups.ts` | New, additive file defining `SEO_NAV_GROUPS` (7 ordered group definitions) and `SEO_EXTRA_NAV_ITEMS` (the 5 previously-hardcoded pages, now with stable ids). References `SEO_MODULE_REGISTRY` items by id only — does not modify the `SeoModule` type or any other consumer of the registry (`ModulePlaceholderPage.tsx`, `getModuleById`/`getModuleByRoute`). | Navigation IA | **new** — additive; no locked file |
| `src/registry/moduleRegistry.ts` (one label rename) | `name: "Visibility Dashboard"` → `name: "SEO Dashboard"` on the sole matching registry entry, with an explanatory code comment. Internal `id: "visibility-dashboard"` deliberately retained (not user-visible; documented). No other field, entry, or the exported type changed. | Navigation IA | **changed** (2026-07-20) — additive/terminology only; no locked file |
| `src/components/layout/Sidebar.tsx` (rewritten) | Renders SEO as a collapsible top-level module with 7 independently collapsible logical groups, using native-button disclosure (`aria-expanded`/`aria-controls`, no new dependency) and the existing plain-`localStorage` persistence convention (matching `ActiveWebsiteContext.tsx`). Auto-expand-only-never-auto-collapse on the active route. `Header.tsx`/`Layout.tsx` unchanged; the pre-existing `hidden md:block` mobile breakpoint behavior is preserved exactly (no mobile drawer exists or was added). | Navigation IA | **changed** (2026-07-20) — no locked file; no route-path change |
| `src/help/content/startHere.ts` (additive) | New "Finding your way around" heading/paragraph on `getting-started-with-digibility-seo` describing the real grouped navigation structure; `searchAliases` extended with `"visibility dashboard"` (search-only synonym, never displayed) plus `"seo dashboard"`/`"where is the dashboard"`. Content validation re-run PASS, 0 findings, corpus counts unchanged. | Navigation IA | **changed** (2026-07-20) — additive; public/published article unaffected structurally |
| `DIGIBILITY_SEO_END_TO_END_USER_JOURNEY_AND_SUPPORT_BLUEPRINT.md` (one paragraph corrected) | Draft, non-authoritative doc's sidebar-order paragraph updated in place (was stating the old flat order, including "Visibility Dashboard," as present fact) to point to the new authoritative navigation IA doc. `SEO_PRODUCT_BLUEPRINT_REAL_APP.md` — a genuine historical planning record — was deliberately left untouched. | Navigation IA | **changed** (2026-07-20) — draft-doc correction only |

---

## F. Source-of-truth precedence

If two documents disagree about **status**, precedence is:

1. `CURRENT_PROJECT_STATUS.md`
2. `BACKEND_MILESTONE_HANDOFF.md` (backend) / `SERVICE_LAYER_WIRING_PLAN.md` (wiring)
3. The relevant per-stage `_NOTES.md` verification checkpoint
4. Everything else (plans, guides, historical phase notes)

A historical phase note describing "what that phase did" is not a status
conflict — it is a record; treat superseding forward-references as authoritative
for *current* status.

---

## G. Not documentation (do not treat as docs)

`supabase/migrations/*.sql`, `supabase/test/*.sql` (seeds + smoke tests),
`src/**`, config files, `node_modules/`, `dist/` — these are code/data. Read
their in-file header comments when a task touches them, but they are not part of
this documentation index and are governed by the "hard prohibitions" in
`DOCUMENTATION_WORKFLOW_RULES.md` §7.
